import { Box, Typography } from "@mui/material";

export const metadata = {
  title: "Dashbord",
};

export default function Page() {
  return (
    <Box>
      {/* <Typography variant="h3" sx={{ textAlign: "center", p: 2, mt: 8 }}>
        Please login to continue
      </Typography> */}
    </Box>
  );
}
